version: 2

run:
  timeout: 5m
  tests: false

linters:
  enable:
    - govet
    - staticcheck
    - ineffassign
    - misspell

issues:
  exclude-use-default: false
  max-same-issues: 0
  max-issues-per-linter: 0
