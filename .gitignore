# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
clustercost-agent-k8s
/bin/
/build/

# Test binaries, fuzz artifacts, and coverage output
*.test
*.out
*.bench
*.prof
coverage*.txt
coverage*.out

# Go workspace files (keep go.sum committed)
go.work
go.work.sum

# Dependency directories (optional; modules fetch on demand)
vendor/

# IDE/editor noise
.vscode/
.idea/
*.swp

# Helm build artifacts
charts/*.tgz
